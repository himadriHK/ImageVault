<?xml version="1.0" encoding="UTF-8" ?>
<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
        xmlns:local="clr-namespace:ImageVault"
        xmlns:views="clr-namespace:ImageVault.Views"
        x:Class="ImageVault.AppShell"
       FlyoutBehavior="Flyout">

    <FlyoutItem Title="Gallery" Icon="">
        <ShellContent
            ContentTemplate="{DataTemplate local:MainPage}" />
    </FlyoutItem>

    <FlyoutItem Title="Faces" Icon="">
        <ShellContent
            ContentTemplate="{DataTemplate views:FacesPage}" />
    </FlyoutItem>

    <FlyoutItem Title="Settings" Icon="">
        <ShellContent
            ContentTemplate="{DataTemplate local:SettingsPage}" />
    </FlyoutItem>

</Shell>
